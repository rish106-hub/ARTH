<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- Production: HTTPS-only, cleartext blocked everywhere -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!--
      Debug builds: trust user-installed CAs (Charles, mitmproxy)
      and allow localhost cleartext for Flutter hot-reload.
      This block is ignored in release builds.
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
    </debug-overrides>

</network-security-config>
