<?xml version="1.0" encoding="utf-8"?>
<!--
  ARTH Network Security Config
  ─────────────────────────────────────────────────────────────────────────────
  - Blocks all cleartext (HTTP) traffic in production and debug builds.
  - All API calls must use HTTPS (Firebase, googleapis.com, etc.).
  - Certificate pinning can be added per-domain when needed.
  ─────────────────────────────────────────────────────────────────────────────
-->
<network-security-config>

    <!-- Production: HTTPS-only, cleartext explicitly blocked -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs (Google, DigiCert, etc.) -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!-- Firebase / Google APIs — HTTPS enforced -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firestore.googleapis.com</domain>
        <domain includeSubdomains="true">firebase.googleapis.com</domain>
        <domain includeSubdomains="true">firebaseremoteconfig.googleapis.com</domain>
        <domain includeSubdomains="true">fcm.googleapis.com</domain>
        <domain includeSubdomains="true">identitytoolkit.googleapis.com</domain>
    </domain-config>

    <!--
      Debug builds: allow localhost for Flutter DevTools / hot reload.
      This block is ONLY active when the debug APK is built.
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system"/>
            <certificates src="user"/>
        </trust-anchors>
        <domain-config cleartextTrafficPermitted="true">
            <domain includeSubdomains="false">localhost</domain>
            <domain includeSubdomains="false">127.0.0.1</domain>
            <domain includeSubdomains="false">10.0.2.2</domain>
        </domain-config>
    </debug-overrides>

</network-security-config>
